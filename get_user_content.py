import re

with open('message.txt', 'r', encoding='utf-8') as f:
    content = f.read()

# I don't have message.txt. Wait, I can just copy the text from the previous message? No, I can't directly grab it unless I use API.
